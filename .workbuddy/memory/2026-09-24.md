# 2026-09-24

## mdBook → VitePress 迁移完成

- 新增：`package.json`（vitepress + vitepress-plugin-mermaid + mermaid）、`.vitepress/config.mts`（srcDir=src、cleanUrls、本地搜索、editLink 指向 GitHub）、`.vitepress/sidebar.mts`（从 src/SUMMARY.md 自动生成侧边栏）、`src/index.md`（首页）、`src/public/`（CNAME + preface/images 图片）
- 章节 README.md 通过 config 里的 `collectReadmeRewrites` 重写为 index.md，URL 为干净目录路径
- 内容修复 2 处：`src/preface/support.md` 的 img 改为 `/preface/images/*.png` 绝对路径；`src/part1-fundamentals/ch04-memory/01-android-linux-memory-overview.md` 一个指向仓库外本地文件的死链改为纯文本
- 构建产物 `.vitepress/dist/`（318 页）；`vitepress dev` 在本机沙箱下启动失败，用 `npm run docs:build` 后 `vitepress preview --port 4173` 预览
- mdBook 的 book.toml/theme/ 保留未删；SUMMARY.md 仍被 sidebar 生成器依赖，不要删
- 沉淀为用户级技能：~/.workbuddy/skills/mdbook-to-vitepress/

## 部署上线：iaimer.com/wiki

- 部署目标：阿里云 47.96.4.59，`/var/www/wiki/`，挂在既有 iaimer.com 站点的 `/wiki/` 路径下（nginx 配置在 `/etc/nginx/conf.d/iaimer.conf` 的 443 server 内）
- **为什么不用 wiki.androidperformance.com**：该域名备案接入在腾讯，解析到阿里云会被 ICP 拦截（403 beian-block）；iaimer.com 已有阿里云接入备案，子路径方案免 DNS 改动
- VitePress `base: '/wiki/'`（子路径部署必须）；更新流程：`npm run deploy`（构建 + rsync 到服务器）
- HTTPS：iaimer.com 原 DNSPod 证书 2026-09-11 过期；先换过 Let's Encrypt（certbot），后按用户最终决定改用其 DNSPod 新证书（TrustAsia DV，2026-09-24 ~ 2026-12-23，含 www，文件在 /etc/nginx/iaimer.com_bundle.crt 与 iaimer.com.key）。**注意：90 天手动续期，2026-12-23 前需用户重新申请并替换**；certbot 的 LE 配置保留在 /etc/letsencrypt/ 可随时切回
- 遗留：/etc/nginx/conf.d/wiki.conf（server_name wiki.iaimer.com 的 80 端口 vhost）处于休眠状态，若日后加 DNS 可启用；androidperformance.com 若做阿里云接入备案可切正式域名
- 坑：沙箱 safe-delete 拦截会导致 `vitepress build` 在清理 .temp 时中途死掉、产物不更新（表现为构建"成功"但 dist 是旧的）；构建后务必检查 dist/index.html 里的资源路径
- 旁发现：iaimer.com 主站 502（Next.js :3000 容器没跑）；腾讯轻量云 124.221.227.163 SSH 在 kex 阶段被断（fail2ban 或 sshd 异常），未能登录
